# Algorithm

控制和数据处理算法集合，不直接负责业务模式或硬件中断分发。

## 模块

| 目录 | 作用 |
| --- | --- |
| [`math_tools`](math_tools/README.md) | 单位换算、数值映射、角度归一化等工具 |
| [`pid`](pid/README.md) | 带限幅、死区、变速积分、积分分离和微分先行的 PID |

## 使用原则

- 算法的 `dt`、单位和坐标系由调用者统一。
- 初始化和动态内存分配应在控制循环启动前完成。
- 参数默认值不代表适用于其他机器人，移植后需要重新标定。
